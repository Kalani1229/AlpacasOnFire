# 施工 Prompt — 第一批：手提箱與擺攤系統

> 把底下整段（從「## 你的任務」開始）複製給 agent。

---

## 你的任務

在既有專案《羊駝很忙》Phase 1 的基礎上，實作**「擺攤系統」**：
玩家帶著一個手提箱在世界中移動，找個地方打開箱子 ——
**所有裝備會彈出到襯布上**，玩家可以在網格上自由重新陳列，
按下「開張」後在限時內生產衣服並從交貨窗口出貨。

參考體驗：**PlateUp!** 的廚房佈置 —— 網格吸附、四向旋轉、營業前自由調整動線。

這是遊戲方向轉向後的第一批改動 —— 原本「固定工坊 + 三分鐘關卡」的設定，
要改成「機台裝在手提箱裡，可以在任何地方架起攤位」。

**本批完成後應該是一個可以完整跑通、可以評估好不好玩的版本**（只是還沒有顧客 NPC）。

---

## 範圍邊界

**本批要做**：手提箱、襯布網格、裝備彈出與網格陳列、機台改造成可攜帶裝備、
交貨窗口、輸送帶、擺攤狀態機、開張按鈕與多人同步、擺攤計時與結算、資本額累積、
測試場景與自我診斷。

**本批明確不做**（不要自作主張加進來）：
- 顧客 NPC（下一批才做，但架構要留得住）
- 城市地圖、區域解鎖、地點好壞的判定
- 素材有限化（素材維持現在工坊場景的取得方式）
- 展示架自售
- 機台的取得流程與手提箱升級（一開始就給全部裝備）
- 背包系統（**明確不做**，手提箱就是取代它的東西）
- 裝備選單／購買 UI（**不需要**，開箱時全部裝備直接彈出）

---

## 絕對不要動的東西

以下系統已經驗證可用，**不要重寫、不要「順手改進」**：

- Photon Fusion 的連線架構（`GameLauncher`、`NetInput`、`SpawnPointRegistry`）
- 玩家移動與共用朝向攝影機（`PlayerController`、`PlayerCameraRig`、`LocalInputProvider`）
- 丟接系統（`PlayerCarry`、`CarriableItem` 的飛行與接住邏輯）
- `IInteractable` / `IItemUser` / `IGarmentHostUser` / `IHoldTool` 互動架構
- 各機台的**內部生產邏輯**（`SewingMachine`、`Juicer`、`SprayGunTool`、`Mannequin` 等）
- 關卡編輯器與 `LevelDefinition` 資料格式
- `Core/GameTuning.cs` 的既有數值（可新增，不要改動既有的）

**新功能一律以新的 `IInteractable` 實作接上去，不要修改既有物件的程式碼。**

---

## 已定案的設計決策

1. **手提箱取代背包系統。** 不做背包 UI，也不做裝備選單。所有裝備裝在手提箱裡。
2. **開箱 = 襯布展開 + 所有裝備彈到各自的位置**（PlateUp 式），
   玩家接著可以自由重新陳列。
3. **襯布就是網格板。** 裝備吸附到格子，只能在襯布範圍內。
4. **四個朝向**（0° / 90° / 180° / 270°），沒有自由角度。
5. **手提箱會記住上次的佈局。** 收攤時記錄每台裝備的格子座標與朝向，
   下次開箱原樣彈出 —— 玩家調好的動線跟著他走。
6. **交貨採 Overcooked 模式**：擺一個**交貨窗口**，玩家把做好的衣服拿去窗口交貨。
   顧客（下一批）拿到衣服就走，**不會當場穿上**。
7. **訂單系統沿用現有的 `OrderBoard`**，需求從「當前可能的成品組合」隨機抽。
8. **本批交貨直接交「成品衣服」，不使用箱子。** `BoxItem` 與裝箱流程的程式碼保留但本批不使用。
9. **計時從關卡搬到擺攤階段。** 探索不限時，擺攤限時。
10. **開張之後全員顧攤，不能繼續探索。**
11. **輸送帶只是一條方便的輸送線**，不做自動化系統。
12. 扛手提箱的人**手就滿了**（沿用「手上一次只能拿一件」的設定）。

---

## 詳細規格

### 一、手提箱（Suitcase）

- 新增 `ItemKind.Suitcase`，實作為 `CarriableItem` 的子類
- 拿著時佔用雙手，與其他物品互斥
- **開箱**：空手拿著手提箱、面向地面按 Space
  1. 在玩家前方展開襯布（對齊玩家當下的 yaw，吸附到最接近的 90 度）
  2. **所有裝備依序彈出**到襯布上各自的格子（給一個短暫的彈出動畫／音效，這是有魅力的一刻）
  3. 手提箱本體落在襯布邊緣
- **裝備彈出的位置來源**：
  - 手提箱裡存著一份**佈局資料**（每台裝備的格子座標 + 朝向）
  - 第一次使用時用**預設佈局**（要能跑通白色 T-shirt 產線的合理排法）
  - 之後每次收攤都會更新這份佈局 → **下次開箱原樣彈出**
- **收攤**：對著手提箱按 Space
  1. 記錄目前所有裝備的格子座標與朝向到佈局資料
  2. 裝備收回、襯布收起、箱子回到手上
- 開箱前要檢查前方是否有**足夠平坦且淨空**的地面容納整塊襯布，
  不合格要給明確提示（例如襯布預覽整塊變紅）

### 二、襯布 = 網格板（StallMat）

- **6 × 6 格，每格 1.5 公尺**（＝ 9 × 9 公尺）。數值放進 `GameTuning`
- **襯布是一個平面**，所有裝備站在這個平面上 ——
  **裝備不需要各自做地面檢測**，只有開箱時需要檢查地面
- 貼地顯示，半透明色塊 + 邊框，一眼看得出範圍
- **佈置模式時顯示網格線**，營業模式時隱藏
- 襯布尺寸必須保證**放得下全部裝備**（這是設計約束，不要做「裝不下就留在箱子裡」的處理）

### 三、裝備與網格（DeployableDevice）

新增一個 `DeployableDevice` 元件掛在裝備 prefab 上，負責擺放狀態。
**機台自己的生產邏輯完全不動。**

- 每台裝備佔用 **w × d 格**。v1 全部設為 **1 × 1**，
  但資料結構要支援更大的佔地（之後可能有 2×1 的交貨櫃台）
- **朝向只有四個**：0° / 90° / 180° / 270°
- 朝向會影響：
  - **輸送帶**的輸送方向
  - **交貨窗口**的窗口面向（顧客會排在窗口外側）
  - 各機台的**操作面**（玩家該站哪一側）
- **格子座標與朝向用 `[Networked]` 的整數同步**，不要同步浮點座標 ——
  整數不會有誤差，兩端一定一致

### 四、佈置模式 vs 營業模式

用模式切換避免新增按鍵：

| 模式 | 時機 | Space 的意義 |
|---|---|---|
| **佈置模式** | 開箱後、開張前 | 拿起／放下裝備 |
| **營業模式** | 按下開張後 | 正常操作機台（放羊毛、選版型……） |

**佈置模式的操作**：

- 面向某台裝備按 **Space** → 拿起來，變成跟著玩家準心投影在網格上的幽靈
- **滑鼠滾輪** → 旋轉 90°
- **Space** → 放進目前高亮的格子
- **Q** → 取消，裝備回到原本的位置
- 目標格子要高亮；不合法時變紅並顯示原因：
  - 超出襯布範圍
  - 該格已被其他裝備佔用
- **不需要任何裝備選單 UI** —— 所有裝備開箱時就已經在場上了

### 五、機台改造

把現有機台從「場景固定物」改成「可從手提箱彈出的裝備」：

- 剃毛器、縫紉機、果汁機、噴槍、人偶、**交貨窗口**、輸送帶
- **它們的生產邏輯完全不動**，只透過 `DeployableDevice` 加上擺放能力
- 不要去改機台自己的程式碼

### 六、交貨窗口（DeliveryCounter）

- 一台可擺放的裝備，**朝向決定窗口面向哪邊**
- 玩家**手持成品衣服**面向它按 Space → 交貨
- 沿用 `OrderBoard.TryDeliver()` 做訂單比對與計分（成功／失敗的處理維持現狀）
- 請在窗口外側預留一個 `CustomerQueueAnchor` 空物件標記排隊位置
  （**下一批的顧客 NPC 會用到**）

### 七、輸送帶（Conveyor）

- 可擺放，**輸送方向由朝向決定**
- 放在上面的 `CarriableItem` 會被往前推，到末端掉落
- 由狀態權威在 `FixedUpdateNetwork` 推動位置
- v1 不需要接進機台的輸入口，能把東西送到定點就好
- 相鄰且同向的輸送帶應該能自然接續

### 八、擺攤狀態機

```
Exploring（探索中）→ Deploying（佈置）→ Open（營業中）→ Settling（結算）→ Exploring
```

- `Exploring`：可自由移動，沒有計時
- `Deploying`：開箱之後。可重新陳列裝備。**沒有計時**
- `Open`：**只有房主**可以按「開張」進入。開始計時，切換成營業模式，不能移動裝備
- `Settling`：計時結束，跳出結算，之後回到 `Exploring`（可收攤搬家）

狀態要是 `[Networked]`，所有玩家一致。

### 九、計時與結算

- 一場營業 **180 秒**（沿用 `GameTuning.LevelDurationSeconds` 的既有機制）
- 結算畫面顯示：**本場營業額、成交筆數、錯過的訂單數**
- 本場營業額累加到一個**跨場次的資本額**（`[Networked]`，先存在記憶體，不需要做存檔）
- 現有的星級結算**先關閉但不要刪除程式碼**

### 十、測試場景與自我診斷

- 提供一個測試場景，包含地板、素材點、出生點、一個手提箱
- 提供**一鍵重置**（選單指令或遊戲內按鍵）
- 沿用專案裡既有的 `BuildReport` 機制，把以下都記錄下來：
  開箱時的地面檢查結果、佈局資料的存取、放置驗證失敗的原因、狀態機轉換

### 十一、數值

全部放進 `Core/GameTuning.cs`，並在完成後的回覆中列出你設定的實際數字：

- 網格單格大小（建議 1.5 公尺）
- 襯布格數（建議 6 × 6）
- 開箱所需的地面平坦度容許角度
- 裝備彈出的動畫時間
- 輸送帶速度
- 擺攤時長（180 秒）

---

## 技術注意事項（這些是本專案踩過的坑，請務必遵守）

1. 角色移動**必須維持使用 Fusion 的 `NetworkCharacterController`**，
   不可以改回原生 `CharacterController` + `NetworkTransform`（用戶端會抖動並瞬間大步移動）。
   任何瞬移一律走 `NetworkCharacterController.Teleport()`。
2. Editor 端**不要用 `AssetDatabase.StartAssetEditing()`** 包住會觸發資產匯入的操作
   （`SaveAsPrefabAsset`、`CreateAsset`），會在不固定的位置丟例外。
3. Editor 端**不要跨 `NewScene` / `OpenScene` 持有 ScriptableObject 參考** ——
   場景載入會卸載資產、留下 Unity 的「假 null」。一律用資產路徑重新載入。
4. 用程式修改 prefab instance 之後，必須呼叫
   `PrefabUtility.RecordPrefabInstancePropertyModifications()`，否則存檔時會被丟掉。
5. `using static` 匯入的方法**不要跟 Unity 型別同名**（曾發生 `Material` 造成的 CS1955）。
6. 所有新狀態**從一開始就寫成 `[Networked]`**，在 `FixedUpdateNetwork` 裡模擬，
   不要先寫成一般 MonoBehaviour 之後再改。
7. 新增的可調數值一律放進 `Core/GameTuning.cs`，不要散落各處。
8. 開發期用 `GameMode.Single` 驗證，核心邏輯確認沒問題後再測連線。

---

## 驗收標準

- [ ] 拿著手提箱走到空地按 Space，襯布展開、**所有裝備彈到各自的格子上**
- [ ] 地面不夠平或空間不足時，開箱會被擋下並給出明確提示
- [ ] 佈置模式下能拿起裝備、用滾輪轉 90 度、放到別的格子；目標格會高亮
- [ ] 放到襯布外或已被佔用的格子會變紅並被擋下，且顯示原因
- [ ] **收攤後換個地方重新開箱，佈局跟收攤前一模一樣**
- [ ] 房主按「開張」後進入營業模式，計時開始，裝備不能再移動
- [ ] **在擺出來的攤位上能完整跑通白色 T-shirt 產線並從交貨窗口出貨得分**
- [ ] 紅色 T-shirt 產線（染料 → 果汁機 → 噴槍 → 人偶）在攤位上同樣跑得通
- [ ] 輸送帶能依朝向把物品送到另一端，相鄰同向的輸送帶能接續
- [ ] 計時結束跳出結算，顯示營業額、成交筆數、錯過的訂單數，並累加到資本額
- [ ] 兩個連線的用戶端上，襯布、裝備的格子位置與朝向、擺攤狀態、計時、營業額都正確同步
- [ ] 現有的移動、攝影機、丟接、產線邏輯**沒有退步**

---

## 建議實作順序

1. 手提箱物件 + 襯布展開／收起（先不管裝備）
2. 網格系統：格子座標 ↔ 世界座標的轉換、佔用表、四向朝向
3. `DeployableDevice` + 開箱時的批次彈出（先用預設佈局）
4. 佈置模式的拿起／旋轉／放下，含驗證與高亮
5. 佈局資料的記錄與還原（收攤 → 開箱原樣重現）
6. 把現有機台接上 `DeployableDevice`（生產邏輯不動）
7. 交貨窗口 + 接上 `OrderBoard`
8. 擺攤狀態機 + 開張按鈕 + 計時與結算
9. 輸送帶
10. 測試場景、一鍵重置、`BuildReport` 診斷
11. 連線測試

完成後請列出你實際設定的所有數值，以及**你改動了哪些既有檔案**（我需要確認沒有動到不該動的部分）。
