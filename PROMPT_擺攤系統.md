# 施工 Prompt — 第一批：手提箱與擺攤系統

> 把底下整段（從「## 你的任務」開始）複製給 agent。

---

## 你的任務

在既有專案《羊駝很忙》Phase 1 的基礎上，實作**「擺攤系統」**：
玩家帶著一個手提箱在世界中移動，找個地方打開箱子、把機台擺出來、按下「開張」，
在限時內生產衣服並從交貨窗口出貨。

這是遊戲方向轉向後的第一批改動 —— 原本「固定工坊 + 三分鐘關卡」的設定，
要改成「機台裝在手提箱裡，可以在任何地方架起攤位」。

**本批完成後應該是一個可以完整跑通、可以評估好不好玩的版本**（只是還沒有顧客 NPC）。

---

## 範圍邊界

**本批要做**：手提箱、襯布範圍、機台擺放／旋轉／收回、機台改造成可攜帶裝備、
交貨窗口、輸送帶、擺攤狀態機、開張按鈕與多人同步、擺攤計時與結算、資本額累積、
測試場景與自我診斷。

**本批明確不做**（不要自作主張加進來）：
- 顧客 NPC（下一批才做，但架構要留得住）
- 城市地圖、區域解鎖、地點好壞的判定
- 素材有限化（素材維持現在工坊場景的取得方式）
- 展示架自售
- 機台的取得流程與手提箱升級（一開始就給全部機台）
- 背包系統（**明確不做**，手提箱就是取代它的東西）

---

## 絕對不要動的東西

以下系統已經驗證可用，**不要重寫、不要「順手改進」**：

- Photon Fusion 的連線架構（`GameLauncher`、`NetInput`、`SpawnPointRegistry`）
- 玩家移動與共用朝向攝影機（`PlayerController`、`PlayerCameraRig`、`LocalInputProvider`）
- 丟接系統（`PlayerCarry`、`CarriableItem` 的飛行與接住邏輯）
- `IInteractable` / `IItemUser` / `IGarmentHostUser` / `IHoldTool` 互動架構
- 各機台的**內部生產邏輯**（`SewingMachine`、`Juicer`、`SprayGunTool`、`Mannequin` 等）
- 關卡編輯器與 `LevelDefinition` 資料格式
- `Core/GameTuning.cs` 的既有數值（可新增，不要改動既有的）

**新功能一律以新的 `IInteractable` 實作接上去，不要修改既有物件的程式碼。**

---

## 已定案的設計決策

1. **手提箱取代背包系統。** 不做背包 UI。所有機台裝在手提箱裡。
2. **打開手提箱時，襯布攤開在地上，那塊布就是擺攤範圍。** 只能在襯布內擺放機台。
3. **交貨採 Overcooked 模式**：擺一個**交貨窗口**，玩家把做好的衣服拿去窗口交貨。
   顧客（下一批）拿到衣服就走，**不會當場穿上**。
4. **訂單系統沿用現有的 `OrderBoard`**，需求從「當前可能的成品組合」隨機抽。
5. **本批交貨直接交「成品衣服」，不使用箱子。** `BoxItem` 與裝箱流程的程式碼保留但本批不使用。
6. **計時從關卡搬到擺攤階段。** 探索不限時，擺攤限時。
7. **開張之後全員顧攤，不能繼續探索。**
8. **輸送帶只是一條方便的輸送線**，不做自動化系統。
9. 扛手提箱的人**手就滿了**（沿用「手上一次只能拿一件」的設定）。

---

## 詳細規格

### 一、手提箱（Suitcase）

- 新增 `ItemKind.Suitcase`，實作為 `CarriableItem` 的子類
- 拿著時佔用雙手，與其他物品互斥
- **空手拿著手提箱、面向地面按 Space** → 開箱：在玩家前方展開襯布，手提箱本體落在襯布中央
- 對著已展開的手提箱按 Space → **收攤**：所有擺出來的機台收回箱中，襯布收起，箱子回到手上
- 開箱前要檢查前方是否有足夠平坦的空地（襯布四角向下射線檢測），不合格要給明確提示

### 二、襯布與擺放範圍（StallMat）

- 方形，預設 **8 × 8 公尺**（放進 `GameTuning`）
- 貼地顯示，半透明色塊 + 邊框，一眼看得出範圍
- 展開方向對齊玩家當下的 yaw
- 機台**只能**放在襯布範圍內

### 三、佈置模式 vs 營業模式（重要：解決按鍵衝突）

擺攤分成兩個模式，**用模式切換避免新增按鍵**：

| 模式 | Space 的意義 |
|---|---|
| **佈置模式**（開箱後、開張前） | 拿起／移動／放下機台 |
| **營業模式**（按下開張後） | 正常操作機台（放羊毛、選版型……） |

- **佈置模式**：
  - 對著手提箱按 Space → 打開機台選單（做法參考現有的 `PatternSelectPanel`，已有先例）
  - 選一台 → 進入**放置預覽**：幽靈模型跟著玩家準心投影在襯布上
  - **滑鼠滾輪**旋轉 90 度、**Space** 確定放置、**Q** 取消
  - 對著已放置的機台按 Space → 拿起來重新放置
  - 放置驗證：必須在襯布內、不與其他機台重疊、地面夠平
  - 不合法時幽靈模型變紅並顯示原因

### 四、機台改造

把現有機台從「場景固定物」改成「可從手提箱擺出來的裝備」：

- 剃毛器、縫紉機、果汁機、噴槍、人偶、**交貨窗口**、輸送帶
- **它們的生產邏輯完全不動**，只加上「可被擺放／收回」的能力
- 建議做法：新增一個 `DeployableDevice` 元件掛在機台 prefab 上，
  負責擺放狀態、收回、放置驗證；不要去改機台自己的程式碼

### 五、交貨窗口（DeliveryCounter）

- 一台可擺放的裝備
- 玩家**手持成品衣服**面向它按 Space → 交貨
- 沿用 `OrderBoard.TryDeliver()` 做訂單比對與計分（成功／失敗的處理維持現狀）
- 造型上要有明確的「窗口」朝向（**下一批顧客會排在窗口外側**），
  請預留一個 `CustomerQueueAnchor` 的空物件標記排隊位置

### 六、輸送帶（Conveyor）

- 可擺放，有明確方向
- 放在上面的 `CarriableItem` 會被往前推，到末端掉落
- 由狀態權威在 `FixedUpdateNetwork` 推動位置
- v1 不需要接進機台的輸入口，能把東西送到定點就好

### 七、擺攤狀態機

```
Exploring（探索中）→ Deploying（佈置）→ Open（營業中）→ Settling（結算）→ Exploring
```

- `Exploring`：可自由移動，沒有計時
- `Deploying`：開箱之後。可擺放機台。**沒有計時**
- `Open`：**只有房主**可以按「開張」進入。開始計時，切換成營業模式，不能移動機台
- `Settling`：計時結束，跳出結算，之後回到 `Exploring`（可收攤搬家）

狀態要是 `[Networked]`，所有玩家一致。

### 八、計時與結算

- 一場營業 **180 秒**（沿用 `GameTuning.LevelDurationSeconds` 的既有機制）
- 結算畫面顯示：**本場營業額、成交筆數、錯過的訂單數**
- 本場營業額累加到一個**跨場次的資本額**（`[Networked]`，先存在記憶體，不需要做存檔）
- 現有的星級結算**先關閉但不要刪除程式碼**

### 九、測試場景與自我診斷

- 提供一個測試場景，包含地板、素材點、出生點、一個手提箱
- 提供**一鍵重置**（選單指令或遊戲內按鍵）
- 沿用專案裡既有的 `BuildReport` 機制，把擺放驗證失敗的原因、
  機台擺放／收回、狀態機轉換都記錄下來 —— 這在除錯時非常重要

### 十、數值

全部放進 `Core/GameTuning.cs`，並在完成後的回覆中列出你設定的實際數字：

- 襯布尺寸（建議 8 × 8 公尺）
- 機台之間的最小間距
- 地面平坦度的容許角度
- 擺放／收回所需時間（建議瞬間，先不做長按）
- 輸送帶速度
- 擺攤時長（180 秒）

---

## 技術注意事項（這些是本專案踩過的坑，請務必遵守）

1. 角色移動**必須維持使用 Fusion 的 `NetworkCharacterController`**，
   不可以改回原生 `CharacterController` + `NetworkTransform`（用戶端會抖動並瞬間大步移動）。
   任何瞬移一律走 `NetworkCharacterController.Teleport()`。
2. Editor 端**不要用 `AssetDatabase.StartAssetEditing()`** 包住會觸發資產匯入的操作
   （`SaveAsPrefabAsset`、`CreateAsset`），會在不固定的位置丟例外。
3. Editor 端**不要跨 `NewScene` / `OpenScene` 持有 ScriptableObject 參考** ——
   場景載入會卸載資產、留下 Unity 的「假 null」。一律用資產路徑重新載入。
4. 用程式修改 prefab instance 之後，必須呼叫
   `PrefabUtility.RecordPrefabInstancePropertyModifications()`，否則存檔時會被丟掉。
5. `using static` 匯入的方法**不要跟 Unity 型別同名**（曾發生 `Material` 造成的 CS1955）。
6. 所有新狀態**從一開始就寫成 `[Networked]`**，在 `FixedUpdateNetwork` 裡模擬，
   不要先寫成一般 MonoBehaviour 之後再改。
7. 新增的可調數值一律放進 `Core/GameTuning.cs`，不要散落各處。
8. 開發期用 `GameMode.Single` 驗證，核心邏輯確認沒問題後再測連線。

---

## 驗收標準

- [ ] 拿著手提箱走到空地，按 Space 能展開襯布，襯布範圍一眼看得出來
- [ ] 能從手提箱選出機台、預覽、旋轉、放置；放到襯布外或重疊時會被擋下並顯示原因
- [ ] 能把已放置的機台拿起來重新擺放，也能整箱收攤
- [ ] 房主按「開張」後進入營業模式，計時開始，機台不能再移動
- [ ] **在擺出來的攤位上能完整跑通白色 T-shirt 產線並從交貨窗口出貨得分**
- [ ] 紅色 T-shirt 產線（染料 → 果汁機 → 噴槍 → 人偶）在攤位上同樣跑得通
- [ ] 輸送帶能把物品從一端送到另一端
- [ ] 計時結束跳出結算，顯示營業額、成交筆數、錯過的訂單數，並累加到資本額
- [ ] 結算後能收攤、換個地方重新擺攤
- [ ] 兩個連線的用戶端上，襯布、機台位置、擺攤狀態、計時、營業額都正確同步
- [ ] 現有的移動、攝影機、丟接、產線邏輯**沒有退步**

---

## 建議實作順序

1. 手提箱物件 + 襯布展開／收起（先不管機台）
2. `DeployableDevice` + 佈置模式的放置／旋轉／收回，含驗證與提示
3. 把現有機台接上 `DeployableDevice`（生產邏輯不動）
4. 交貨窗口 + 接上 `OrderBoard`
5. 擺攤狀態機 + 開張按鈕 + 計時與結算
6. 輸送帶
7. 測試場景、一鍵重置、`BuildReport` 診斷
8. 連線測試

完成後請列出你實際設定的所有數值，以及**你改動了哪些既有檔案**（我需要確認沒有動到不該動的部分）。
