# v6 批 A — 測試清單

這一批只做了四件事：**隨身剃毛器、會走動的羊毛 NPC、剃毛進共用背包、背包 HUD**。
素材箱、織布機、顧客是批 B，這次沒有。

先跑第 0 與第 1 節（回歸），**回歸沒過就不要往下測** —— 那代表舊場景被弄壞了，
先修好再看新東西。

---

## 0. 建置（順序不能換）

- [ ] `羊駝很忙 / 1. 建置佔位資產` → Console 沒有紅字
- [ ] **`Tools > Fusion > Rebuild Prefab Table`** → 出現 `Rebuild Prefab Table done.`
- [ ] `羊駝很忙 / v6 羊駝村 / 3. 檢查羊駝村設置` → 顯示「批 A 設置完整」並印出五色羊毛價格
- [ ] `羊駝很忙 / v6 羊駝村 / 1. 建置羊駝村測試場景` → Console 沒有紅字

> 診斷選單會特別檢查 NPC prefab **有 NCC、沒有 NetworkTransform**。
> 兩個同時存在會互相打架，連線時 NPC 會抖動 —— 這是玩家身上踩過的同一個坑。

---

## 1. 回歸：`Stall_Test` 不能有任何退步（最重要）

打開 `Scenes/Stall_Test.unity` 按 Play，整條線跑一次：

- [ ] 開箱 → 裝備彈出 → 敲鈴開張
- [ ] 剃毛 → 縫紉機 → 輸送帶 → 交貨窗口，**白 T 恤出貨得分**
- [ ] **紅 T 恤也要出得掉**：果汁機榨染劑 → 塗到人偶身上的衣服 → 取下 → 交貨
- [ ] 訂單卡的文字跟以前一樣（例如「紅T-shirt」，**不能**變成「紅底白紋T-shirt」）
- [ ] `羊駝很忙 / 擺攤 / 3. 檢查擺攤設置` 沒有新的錯誤

> **為什麼特別要測紅 T 恤**：這一批給 `GarmentSpec` 加了「點綴色」，而訂單比對現在
> 也會比點綴色。塗抹只改主色、不動點綴色的話，塗成紅色的衣服會被當成「紅底白紋」，
> 跟訂單要的純紅比對不起來。我在 `Mannequin.ApplyPaintedColor` 補了三行讓點綴色跟著走 ——
> 這一條就是在驗那三行。**紅 T 恤交不出去 = 這一批最可能壞的地方。**

> 訂單卡文字若真的變成「X 底 Y 紋」，代表 `HasAccent` 的守衛沒生效，回報給我。

---

## 2. 新功能：隨身剃毛器（E）

在 `Village_Test` 測。

- [ ] 空手按 **E** → 手上出現剃毛器，HUD 左下顯示「手上：剃毛器」
- [ ] 再按一次 **E** → 剃毛器**消失**（不是掉在地上）
- [ ] 手上拿著別的東西時按 E → 畫面中央出現「先空出手才能拿剃毛器」，**不是靜默沒反應**
- [ ] 拿著剃毛器仍然可以用 Q 丟出去（既有的丟接沒有被影響）

---

## 3. 新功能：會走動的羊毛 NPC

- [ ] 場上有 **10 隻** NPC，白／黃／綠／藍／紅**各 2 隻**，身上的毛是對應顏色
- [ ] NPC 會自己走動：走一段 → 站著發呆 1.5–4 秒 → 再走
- [ ] NPC 不會走出出生點周圍 18 公尺
- [ ] 身上有 **3 撮毛**，數量跟剩餘毛量一致
- [ ] 空手走近 NPC → **沒有互動提示**（不拿剃毛器就不該跳字）

**剃毛**

- [ ] 拿剃毛器走近 → 提示「[Space] 剃紅毛（3/3）」
- [ ] Space → 少一撮毛、**NPC 立刻背對你跑走**約 4 秒、有剃毛音效
- [ ] **羊毛不會掉在地上** —— 直接進背包（這是 v6 跟舊版最大的差別）
- [ ] 剃到 0 → 身體轉成**灰白素體**，遠遠就看得出牠沒毛了
- [ ] 毛剃光後互動 → 提示「牠身上的毛剃光了」，**不是無反應**
- [ ] 等 8 秒 → 長回一撮，再 8 秒又一撮，最多回到 3

---

## 4. 新功能：共用背包與 HUD

- [ ] 畫面左下角有一排 **5 個色塊**（白／黃／綠／藍／紅），上面標題寫「背包」
- [ ] 沒有存量的顏色是**壓暗的**
- [ ] 剃到毛的瞬間，對應色塊**放大一下再縮回去**，數字 +1
- [ ] 剃不同顏色的 NPC → 對應色塊各自累加，互不影響
- [ ] `Stall_Test` 裡**不該出現**這排色塊（那個場景沒有 TeamStash，HUD 要自己隱藏）

---

## 5. 連線（前面全過才做）

用 ParrelSync 開兩個 Editor 實例。

- [ ] 兩邊看到的 NPC 位置一致，**沒有抖動、沒有瞬間大步移動**
- [ ] A 剃毛 → **B 也看到那隻 NPC 少一撮毛、開始逃跑**
- [ ] 兩邊的背包數字一致
- [ ] Client 端按 E 拿剃毛器正常，剃毛正常
- [ ] 玩家移動、攝影機、丟接跟以前一樣

> NPC 抖動或瞬移 = `NetworkCharacterController` 沒生效或 prefab 上多了 `NetworkTransform`。
> 回頭跑第 0 節的診斷選單。

---

## 這一批**不會**有的東西（不是 bug）

- 素材箱、織布機、顧客 —— 批 B
- 手提箱裡還是舊的 Classic 內容（剃毛器架、果汁機、人偶）——
  loadout 拆分排在批 B，批 A 用不到
- NPC 不會來買東西、也不會排隊
- 背包裡的毛目前**還沒有用途**（批 B 的素材箱才會消耗它）

---

## 回報時請附上

1. 卡在哪一節哪一項
2. Console 完整紅字
3. `AlpacasOnFire_BuildReport.txt`（第 0 節出問題時特別有用）
4. 如果是 NPC 行為怪：牠當時在走、在站、還是在逃跑
