# 施工指令：操作簡化（左鍵吃掉丟出）

> 貼給新對話即可。只動輸入層，不動任何機台或玩法邏輯。

---

## 目標

把即時操作全部收斂到左鍵。改完之後的完整操作只有：

| 輸入 | 用途 |
|---|---|
| WASD / 方向鍵 | 移動 |
| 滑鼠移動 | 視角（共用朝向） |
| **左鍵 / Space** | **所有即時動作**：接住、互動、丟出 |
| 右鍵 | 設定類動作：手提箱選材料、放置模式取消 |
| E | 拿出／收起剃毛器 |
| 滾輪 | 放置模式轉 90° |

---

## 規則

左鍵按下時，依序判斷，**第一個成立的就執行、不往下跑**：

```
1. 接得到滯空中的東西        -> 接住            （現況，不動）
2. 互動範圍內找得到有效目標  -> 互動            （現況，不動）
3. 手上有東西，而且互動範圍內「完全沒有任何互動物」-> 丟出   ← 新增
4. 其他                      -> 什麼都不做（播一聲拒絕音）
```

### 第 3 條的條件是關鍵，不要寫錯

**「沒有任何互動物」不等於「互動失敗」。**

機台就在面前但滿了 / 成品還沒被拿走時，`CanInteract` 會回 false。
這種情況**必須落到第 4 條（不做事）**，不能落到第 3 條。
否則玩家會在機台滿的時候把手上的毛往機台方向扔出去 ——
同樣的畫面、同樣的按鍵，因為一個看不見的狀態而產生完全不同的結果。

為什麼這樣切不會犧牲「扔進機台」：
互動範圍 2.5 公尺、丟出射程約 7.8 公尺，兩者本來就分開。
近的機台走過去放，遠的機台才需要丟 —— 而遠的機台不在互動範圍內，
自然會落到第 3 條。距離替玩家做了判斷，這正是要的效果。

---

## 實作

### 1. `Player/PlayerInteractor.cs` 加一個查詢

現有的 `FindTarget()` 已經把 `CanInteract` 為 false 的候選過濾掉了，
所以「沒有目標」和「目標拒絕」從回傳值上分不出來。要另外加一支：

```csharp
/// <summary>
/// 互動範圍內、大致在面前，有沒有「任何」互動物 —— **不看 CanInteract**。
/// 用來區分「面前空無一物」與「面前有東西但現在不能用」。
/// 唯讀，任何端都可以呼叫（HUD 提示要用）。
/// </summary>
public bool HasAnyTargetInRange()
```

實作直接照抄 `FindTarget()` 的迴圈，只是**拿掉 `CanInteract` 那一行**，
距離與面向的判定條件要跟 `FindTarget()` 完全一致（`InteractRange + 0.6f`、`Dot >= 0.2f`），
不然會出現「提示說可以丟、按下去卻不丟」的落差。

### 2. `Player/PlayerController.cs` 改左鍵的分支

目前是：

```csharp
if (pressed.IsSet(GameButton.Interact))
{
    if (!_carry.TryManualCatch())
        _interactor.TryInteract();
}
```

改成接住 → 互動 → 丟出三段。`TryInteract()` 要能回報有沒有真的互動到
（把回傳值改成 `bool`，回傳 `target != null`）。丟出的條件是
**手上有東西 且 沒互動到 且 `!HasAnyTargetInRange()`**，
呼叫既有的 `_carry.HandleThrow(in ctx)`，不要另外寫一套丟出邏輯。

### 3. Q 留著當別名

`GameButton.ThrowCatch` 與 `Q` 的綁定**不要移除**。
它現在是「近距離硬要丟」的逃生口（中間卡著一台機器、想把毛丟到後面的輸送帶）。
不寫進教學，之後試玩發現沒人按再拿掉。

### 4. 放置模式的取消搬到右鍵

`Stall/PlayerStallAgent.cs` 目前提示字是
`[Space] 放下…　[滾輪] 轉 90°　[Q] 取消`，取消綁在 Q。
改成右鍵取消，提示字同步改成 `[右鍵] 取消`。
理由：取消是設定類動作，歸右鍵才符合「左鍵即時、右鍵設定」的規則。

### 5. HUD 提示

`UI/GameHud.cs` 在第 3 條成立時要顯示 `[左鍵] 丟出 {物品名}`，
玩家才知道現在按下去會發生什麼。第 4 條（面前有東西但不能用）
維持顯示該物件原本的拒絕提示（例如「已經放滿 2 份了」），不要顯示丟出。

---

## 驗收

1. 手上有毛、面前空無一物 → 左鍵丟出去
2. 手上有毛、走到織布機前（機台空的）→ 左鍵放入，**不會丟**
3. 手上有毛、面前的織布機已放滿 2 份 → 左鍵**什麼都不做**，不會把毛扔出去
4. 手上有毛、面前的機台有成品沒人拿 → 同上，不會丟
5. 站遠一點（超過 2.5 公尺）對著織布機按左鍵 → 丟過去，扔進機台照常成立
6. 有東西朝我飛來 → 左鍵仍然優先解讀成接住
7. Q 仍然可以丟（含近距離）
8. 放置模式：右鍵取消，提示字正確；滾輪轉向不變
9. HUD 在會丟的時候顯示「[左鍵] 丟出 …」，在機台滿的時候顯示拒絕提示
10. 兩個 client 跑一次，client 端的左鍵丟出與放入都正常

---

## 不要動

- 手提箱的 Space 收攤 / 右鍵選材料 —— 兩個都保留，不要合併
- E 剃毛器的綁定
- 任何機台的 `CanInteract` / `Interact` 內容
- `IInteractable` / `ISecondaryInteractable` 介面本身
