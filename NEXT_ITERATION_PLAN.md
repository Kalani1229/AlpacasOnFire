# 下一輪開發計畫 — 給 agent 的施工單

> 目標：在**現有的工坊場景**裡做出「擺攤」的最小可玩版，先驗證這個環節好不好玩。
> **這一輪完全不需要城市。**

---

## 一、必須先決定的事（不決定的話 agent 會自己猜，猜錯就整段重寫）

每一項我都附了建議值。**如果沒意見，直接說「照建議」就可以動工。**

### A. 成交怎麼發生（最大的空缺 —— 目前完全沒定義）

我們把整個遊戲圍繞著「擺攤」設計，但**一筆生意到底怎麼成交，從來沒有寫下來**。
這是新階段的核心動詞，agent 沒有這個就寸步難行。

**建議**：兩種並存，形成一個真的決策 ——

| 方式 | 操作 | 售價 |
|---|---|---|
| **親手交貨** | 客人走到攤位、氣泡顯示需求，玩家拿著對應的衣服面向他按 Space | **正常價** |
| **展示架自售** | 把衣服掛上人偶／衣架，客人自己取走，玩家不用管 | **打折（例如 70%）** |

好處：親手交貨**完全沿用 Phase 1 的 `IInteractable`**，幾乎不用寫新東西；
展示架則直接重用已經做好的**人偶**。而「要顧客人還是去做下一件」變成擺攤時的真實取捨。

### B. 顧客是實體 NPC 還是 UI 訂單

**建議**：實體 NPC + 頭上氣泡（顏色色塊 + 版型圖示）。成交後客人把衣服拿在手上走掉。
不需要任何角色穿衣服的美術。

> 這一題會決定要不要蓋一整套 NPC AI，是本輪最大的分岔，**請務必先拍板**。

### C. 顧客的需求怎麼決定

**建議**：v1 先做「從當前可能的成品組合裡隨機抽」，並保留一個權重介面
（之後接客層、時段、情報時才不用重寫）。

### D. 擺攤的計時與結束

**建議**：一場營業 **3 分鐘**（沿用 Phase 1 已驗證的計時系統），
時間到跳出結算：本場營業額、成交筆數、錯過的客人數。

### E. 手提箱裡有什麼、擺放規則

**建議**：
- v1 一開始就給全部機台（剃毛器、縫紉機、果汁機、噴槍、人偶、箱子點），先不做取得流程
- 打開手提箱 → 襯布攤開在地上 → **只能在襯布範圍內擺放**
- 襯布 v1 固定尺寸（例如 8×8 公尺），升級留到之後
- 擺放：自由位置 + 貼地吸附 + 可用滾輪旋轉 90 度；不做格線
- 可以隨時收回單一機台，也可以整箱收攤

### F. 素材從哪來（這一輪還沒有城市）

**建議**：沿用現有工坊場景的染料點與羊駝身上的羊毛，先不改。
「素材有限化」留到做城市那一輪。

### G. 錢
**建議**：沿用 Phase 1 的 `LevelDirector.Money`，加一個跨場次累積的**資本額**。
星級結算先關掉但不刪除。

### H. 地點好壞的三因素（客層／面積／距離）
**建議：這一輪不做。** 現在只有一個工坊場景，沒有地點可選，做了也測不出東西。
留到城市那一輪。

---

## 二、工作量怎麼抓

### Phase 1 的實證

| 項目 | 數字 |
|---|---|
| 一個 prompt 產出 | 52 個 C# 檔案 |
| 產出後的除錯輪數 | 6 輪 |
| 根本原因 | 4 個，**全部都是「不開 Unity 就驗證不了」的類型** |

**結論：程式碼量不是瓶頸，驗證頻寬才是。**
Agent 可以一次寫 50 個檔案，但你一次只能坐下來測一個晚上。

### 判斷準則

一次施工的大小，應該抓在「**你能在一到兩小時內把它測完**」。
超過這個量，出問題時你會分不清是哪一塊壞的 —— Phase 1 那 6 輪除錯就是這樣來的。

---

## 三、建議分兩次（不是一次，也不用三次）

### 為什麼不建議一次做完

這一輪有一塊風險特別高：**顧客 NPC**。
它牽涉 NavMesh 烘焙、AI 狀態機、Fusion 同步、以及「客人走過來的感覺對不對」這種要反覆調的東西。

如果和擺放系統綁在一起一次做完，客人不會動的時候你會分不清是
「AI 壞了」還是「攤位範圍判定壞了」還是「同步壞了」。

而且**擺放系統是測試顧客的前提** —— 攤位擺不出來，就沒有東西可以賣。

### 第一次：手提箱與擺攤系統

**能單獨驗證的產出：可以打開箱子、擺出機台、跑完產線、收攤。**

- 手提箱物件（可攜帶、佔用雙手、開／收）
- 襯布範圍的判定與視覺
- 機台的擺放／旋轉／收回，含範圍外的擋下與提示
- 機台從場景固定物改造成可擺放裝備（Phase 1 的機台程式邏輯不動）
- 輸送帶（單純的輸送線）
- 擺攤狀態機：探索中 → 開張 → 營業中 → 收攤
- 房主的「開張」按鈕與多人同步
- 一個測試場景，能一鍵重置

估計 15–25 個檔案。**這一批完成後你就能玩到「擺攤」的骨架**，只是還沒有客人。

### 第二次：顧客與營業

**能單獨驗證的產出：完整跑一場三分鐘的營業。**

- 顧客 NPC：生成、走向攤位、氣泡需求、等待、離開
- 成交：親手交貨（Space）＋ 展示架自售
- 客人的耐心與離場（沒被服務到會走掉）
- 擺攤計時與結算畫面
- 營業額 → 資本額累積
- 音效掛點

估計 15–25 個檔案。

### 如果你還是想一次做完

可以，但請在 prompt 裡加這三條防護：

1. **要求做出「無顧客模式」的開關** —— 讓你可以先關掉 NPC 只測擺放系統，
   確認沒問題再打開。這等於把兩批的驗證順序保留下來，但只發一次 prompt。
2. **要求沿用 `BuildReport` 那套自我診斷** —— Phase 1 後期加的那個報告檔幫我們一次抓到問題，
   新系統（擺放驗證、NPC 生成、NavMesh）都應該有同樣的紀錄。
3. **要求提供一個測試場景與一鍵重置**，不要每次都得重開 Unity。

---

## 四、給 agent 的 prompt 要寫進去的注意事項

這些是 Phase 1 踩過的坑，直接寫進 prompt 可以省掉好幾輪：

1. **不要動已驗證可用的系統**：Fusion 連線、共用朝向的角色與攝影機、丟接系統、
   `IInteractable` 架構、關卡編輯器。新功能一律用新的 `IInteractable` 實作接上去。
2. **角色移動必須維持用 `NetworkCharacterController`**，不可以改回原生 CharacterController +
   NetworkTransform（用戶端會抖動與瞬移）；任何瞬移都要走 `Teleport()`。
3. **Editor 端不要用 `AssetDatabase.StartAssetEditing()`** 包住會觸發匯入的操作。
4. **Editor 端不要跨 `NewScene`／`OpenScene` 持有 ScriptableObject 參考**，
   一律用資產路徑重新載入。
5. **用程式改 prefab instance 之後**要呼叫 `PrefabUtility.RecordPrefabInstancePropertyModifications()`。
6. **`using static` 匯入的方法不要跟 Unity 型別同名**（`Material` 那次的 CS1955）。
7. 所有新狀態**從一開始就寫成 `[Networked]`**，在 `FixedUpdateNetwork` 裡模擬。
8. 新增可調數值一律放進 `Core/GameTuning.cs`，不要散在各處。

---

## 五、這一輪之後

擺攤驗證好玩之後，再依序做：

1. 城市第一個街區（blockout，佔位方塊）＋ 探索與擺攤的雙階段切換
2. 地點好壞的三因素（客層／面積／距離）＋ 收攤搬家
3. NPC 偷竊、區域解鎖、跨場次存檔
4. 換上正式美術

**不好玩的話就在這裡改** —— 現在改的成本是最低的。
