# v6 — 羊毛怎麼變成錢：完整設計

> 2026/09/10 更新：素材箱**保留**，但不再是「從選單擺出來的裝備」——
> 在手提箱選了哪三種顏色，場上就直接冒出那三個顏色的箱子，
> **對箱子按右鍵拿毛**。一場的限制是「三種顏色，這三種有多少帶多少」，沒有份數上限。

---

## 一、完整物質流

```
   探索階段（不限時）                  營業階段（180 秒）
   ─────────────────                  ──────────────────

   羊駝 NPC ──剃毛──▶ 共同背包 ──開張時整批裝進──▶ 素材箱 x3（各一色）
   （3 份/隻）        （分色計數）    （該色存量全帶走）      │
        ▲                  ▲                              │ 右鍵拿一份
        │ 8 秒長回一份      │ 收攤時退回剩餘                  │
        │                  └────────────────────           ▼
                                                      手上的羊毛
                                                           │
                                                           ▼
   顧客 ◀──交貨──  交貨窗口 ◀──輸送帶──  織布機（1–2 份，5 秒）
     │                  │
     │ 45 秒耐心         └─▶ 錢 = WoolPrice(主色) + WoolPrice(點綴色)
     └─▶ 逾時走掉，扣款
```

**手提箱只管「決定」，素材箱負責「供料」。** 手提箱是開箱／收攤／選色的入口，
選完之後就退到背景；實際的羊毛在網格上的素材箱裡，而箱子可以搬 ——
所以「材料離織布機多近」是玩家自己排出來的。

---

## 二、按鍵怎麼分工

Space 與右鍵在不同模式下意義不同，靠模式分流，**不新增按鍵**：

**手提箱**（襯布背緣，不佔格子）

| 情境 | Space | 右鍵 |
|---|---|---|
| 探索中（箱子在手上） | 開箱擺攤 | — |
| 佈置模式 | **收攤** | **開啟選色面板** |
| 營業模式 | 不能收攤（提示） | — |

**素材箱**（在網格上，選色後自動出現）

| 情境 | Space | 右鍵 |
|---|---|---|
| 佈置模式 | **拿起來重擺**（DeployableDevice 優先權 4） | — |
| 營業模式 | 只給提示（引導去按右鍵） | **拿一份該色的毛** |

不會打架的理由：佈置模式的 Space 已經是「拿起裝備」，
所以拿毛走右鍵；而佈置模式時箱子還沒裝料，右鍵也沒東西可拿。

### 2.1 選色面板（佈置模式，右鍵）

模態面板，會解鎖游標 —— 沿用 `PatternSelectPanel` 已驗證的寫法。

```
┌─────────── 這一場要帶哪三種毛 ───────────┐
│                                          │
│   ⬜ 白      🟨 黃      🟩 綠            │
│   12 份     8 份      5 份              │
│                                          │
│   🟦 藍      🟥 紅                       │
│   3 份      0 份 ✕                      │
│                                          │
│        已選 2/3　　　[關閉 (Esc)]         │
└──────────────────────────────────────────┘
```

- 每個色塊標注**背包存量** —— 選三色需要同時看到五色的量才決定得了，
  這正是不用循環式介面的理由
- 存量 0 的顏色壓暗、不能選
- 點一下選取／再點取消；選滿三個之後其餘變灰
- 選好的三色顯示在手提箱本體上（三個小色塊），關掉面板也看得到

### 2.2 素材箱：選色即生成

**選了顏色 → 那個顏色的箱子立刻出現在襯布上**（`StallManager.SyncCrates()` 每個 tick 對帳）：

- 選了紅 → 紅箱子冒出來，放在最靠近背緣的空格
- 取消紅 → 剩料退回背包，箱子消失
- 箱子是 `DeployableDevice`，**可以搬到織布機旁邊** —— 動線優化的重點就在這裡
- 位置與顏色都會被佈局記住（顏色存在 `StallSlotRecord.Variant`）

### 2.3 開張瞬間

- 每個素材箱把自己那個顏色的**全部背包存量**裝進來，背包該色歸零
- **沒有上限** —— 有 20 份紅就裝 20 份紅
- 此後鎖定：不能改三色、不能再從背包補貨

### 2.4 營業中拿料

- 走到某個素材箱前面按**右鍵** → 一份該色的毛到手上
- 箱子空了 → 箱體轉灰，提示「空了」
- 一次一份（沿用「手上只能拿一件」的既有規則）

### 2.5 收攤

- 每個素材箱剩下的毛**全部退回背包**
- **三色選擇保留** —— 下次開箱不用重選，跟「記住佈局」同一個精神

---

## 三、這樣改的三個後果

### 3.1 動線變成佈局優化的重點（好）

手提箱固定在襯布**背緣外側**，織布機擺在哪就決定了每一趟要走多遠。

素材箱可以搬，所以搬運距離**由玩家自己決定**。把箱子擺在織布機正旁邊（相鄰格 1.5 公尺），
來回只要不到 1 秒；擺在背緣不動則要跑約 6 公尺。這正是網格擺放要製造的取捨。

下表用「箱子緊鄰織布機」估：

| 衣服 | 搬運 | 織布 | 合計 | 一場（180 秒）產能 |
|---|---|---|---|---|
| 單色（1 份） | 約 1.5 秒 | 5 秒 | 6.5 秒 | 約 27 件 |
| 雙色（2 份） | 約 3 秒 | 5 秒 | 8 秒 | 約 22 件 |

顧客約 15 位 —— 箱子擺得好的話**產能有餘裕**，擺得差就會手忙腳亂。
這比原本「一律要跑回背緣」健康：慢是因為你沒排好，不是系統逼你跑。

輸送帶自動出貨省掉了回程，所以「織布機靠近手提箱、輸送帶負責往前送」
會成為玩家自己發現的最佳解。這正是網格擺放想要的樂趣。

### 3.2 瓶頸從素材移到「搬運 + 織布」（要注意）

移除 18 份上限之後，一場能帶的量 = 三色的背包存量（最多 20×3 = 60 份）。
但一場只做得出 16–22 件 —— **素材不再是限制，時間才是**。

這跟設計文件原本「讓素材成為真正的限制」的意圖不同。實際上限制變成：

1. **短期**：搬運與織布的時間（硬限制）
2. **長期**：你剃了多少毛、剃了哪些顏色（決定收入天花板）

我認為這樣更好：素材決定「**能賺多少**」，時間決定「**賺不賺得完**」，
兩個限制的性質不同，不會互相打架。設計文件擔心的「兩個都緊會讓玩家無所適從」
就不會發生。

**但要注意**：如果玩家剃到三色各 20 份（60 份），素材就完全不是限制了。
測試時留意這是不是讓探索階段變得沒有意義。

### 3.3 三色的取捨要靠稀有度撐起來

五色一樣好取得的話，玩家永遠選紅藍綠（45/30/20），「選三色」是個假決策。
所以 NPC 分布改成**貴的比較少**：

| 顏色 | 單價 | NPC 隻數 | 立即可得 | 說明 |
|---|---|---|---|---|
| 白 | 10 | **3** | 9 份 | 到處都是 |
| 黃 | 15 | **3** | 9 份 | 到處都是 |
| 綠 | 20 | **2** | 6 份 | 普通 |
| 藍 | 30 | **1** | 3 份 | 要找 |
| 紅 | 45 | **1** | 3 份 | 要找，而且要等牠長毛 |

紅毛湊到 6 份大約要 **30 秒**（先剃 3 份，再等 8 秒/份 × 3），白毛則是走過去就有 9 份。

**這個問題已經有解，不用在 v6 處理**（2026/09/10 拍板）：
之後每個地區只會出現**兩種**羊，所以「這一區能做什麼顏色」是地區決定的，
不是玩家在五色裡挑。後期地區的羊毛比前期值錢 —— 值錢的顏色靠**地區進度**
而不是「多花時間剃」來取得，取捨自然就緊了。

批 B 先照上表的 3/3/2/1/1 做，測試場景只是要驗證產線跑得通。

---

## 四、對批 B 施工單的修正

原本的 §4.3「素材箱」整節作廢。改成：

### 素材箱不在 loadout 裡
- `StallLoadout.Village` 的固定裝備是 **4 台**：織布機 ×1、交貨窗口 ×1、輸送帶 ×2
- 素材箱是**選色時動態生成**的，不寫在 `Devices` 也不寫在 `DefaultLayout`
- `StallManager.SyncCrates()` 每個 tick 對帳：選了就生、取消就收
- 沒有「一箱幾份」的上限（`CrateUnitsPerCrate` 已移除，
  只留 `CrateFillBarReference` 給剩餘量條當滿格參考）

### 新增的東西
- `MaterialCrate`：箱體依裝載顏色染色，右鍵拿毛
- `SuitcaseItem` 加：`[Networked] 三色選擇`、`Loaded`（鎖定旗標）
- `SuitcaseColorPanel`：佈置模式的選色面板（模態、解鎖游標）
- `StallSlotRecord` 加 `Variant`：讓箱子的顏色跟著佈局一起被記住
- **右鍵的次要互動分派**（見下）

### 需要的新機制：右鍵次要互動

目前右鍵（`GameButton.UseTool`）只給「手上的 `IHoldTool`」用，
沒有「對準心目標按右鍵」這條路。要加一個 `ISecondaryInteractable`：

```
PlayerController.FixedUpdateNetwork
  └─ pressed.IsSet(UseTool) 且【手上不是 IHoldTool】
       └─ _interactor.TrySecondaryInteract()
            └─ 準心目標若實作 ISecondaryInteractable 就呼叫
```

**「手上不是 IHoldTool」這個條件很重要**：拿著噴槍時右鍵永遠是噴漆，
不會誤觸次要互動。選色／切色時玩家手上是空的或拿著羊毛，都不是 `IHoldTool`，
所以一定觸發得到。

這會動到 `PlayerController` 與 `PlayerInteractor`（兩支都在保護名單上），
但都是純加法：各加一個分支、一個方法。

### 預設佈局（Village loadout，8×8）

```
z=6            [交貨窗口]
z=5            [輸送帶↑]
z=4            [輸送帶↑]
z=3            [織布機]
              x=3

手提箱固定在襯布背緣外（不佔格子），約在 z=-0.9 的位置
```

### 場景：NPC 分布改成 3/3/2/1/1

`VillageSceneBuilder` 目前是五色各 2 隻。批 B 改成上表的稀有度分布。
**批 A 測試維持各 2 隻沒關係** —— 那時候顏色還沒有經濟意義，
平均分配反而比較好找、比較好測剃毛手感。

---

## 五、批 B 的驗收會長這樣

- 佈置模式對手提箱右鍵 → 跳出選色面板，五色各自標注背包存量
- 存量 0 的顏色不能選；選滿三色後其餘變灰
- **選了顏色 → 場上立刻冒出那個顏色的箱子**；取消 → 箱子消失、剩料退回
- 素材箱可以像其他裝備一樣被拿起來重擺，**顏色跟著走**
- 開張 → 每個箱子把該色的**全部存量**裝進來、背包該色歸零、鎖定
- 營業中對箱子按右鍵 → 一份該色的毛到手上
- 某箱用完 → 箱體轉灰、提示「空了」
- 織布機放 1 份 → 單色；放 2 份 → 雙色；**交換順序得到不同的衣服**
- 織布機旁有輸送帶時成品自動出貨，兩條輸送帶會串接
- 顧客要的東西**一定是場上還有存量的素材箱做得出來的**
- 交貨加錢、交錯扣錢、逾時走掉扣錢
- 收攤 → 每個箱子剩下的毛退回背包、**三色選擇與箱子位置都保留到下次開箱**
- `Stall_Test` 完全沒有退步

---

## 六、還沒解決、記在這裡

1. **逾時扣款固定 30，導致挑貴的做沒有意義。**
   服務白衣服顧客賺 10，放掉扣 30 —— 淨差 40 比賺到的還多，
   玩家不能忽略任何生意。建議改成「該訂單價格的 50%」。
   （這一條上一版就提過，還沒拍板。）

2. **主色與點綴色同價，順序沒有經濟意義。**
   「紅底白紋」與「白底紅紋」都是 55 元。建議先不動，玩過再說。

3. ~~探索階段不限時，讓三色的取捨不夠緊。~~
   **已解決**：靠「每個地區只有兩種羊」讓顏色由地區決定，後期地區的毛比前期值錢。
